name: "Philips Avent WebRTC Bridge Dev (Addon)"
description: "WebRTC to RTSP bridge for Philips Avent baby monitors. Dev channel: builds straight off main, no release notes and no promises. Only install this if you were asked to test something specific."
version: "2026.7.0-dev3"
slug: "aventproxy_bridge_dev"
image: "ghcr.io/thekoma/aventproxy-bridge"
url: "https://github.com/thekoma/aventproxy"
arch:
  - amd64
  - aarch64
homeassistant: "2024.1.0"
stage: experimental
host_network: true
apparmor: true
options:
  cameras: []
schema:
  signing_key: str?
  sid: str?
  ecode: str?
  partner: str?
  app_key: str?
  device_id: str?
  package_name: str?
  api_host: str?
  talkback: bool?
  cameras:
    - camera_id: str?
      camera_name: str?
startup: "application"
boot: "auto"
environment:
  WAIT_FOR_CONFIG: "true"
map:
  - config:rw
