#include <tunables/global>

profile aventproxy_bridge flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,
  network inet raw,

  /config/** r,
  /data/** rw,

  /usr/bin/avent-webrtc-bridge ix,
  /usr/bin/jq ix,
  /bin/** ix,
  /usr/bin/** ix,

  /proc/sys/net/** r,
  /sys/kernel/mm/** r,

  /tmp/** rw,
  /run/** rw,
}
