name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: pip install pytest pycryptodome aiohttp voluptuous homeassistant

      - name: Lint
        run: |
          pip install ruff
          ruff check custom_components/ examples/ tools/ --ignore E501

      - name: Test
        run: PYTHONPATH=. pytest -v --tb=short tests/test_philips_avent/

  test-go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"

      - name: Build bridge
        working-directory: tuya-ipc-terminal
        run: go build -o tuya-ipc-terminal .

      - name: Verify binary
        working-directory: tuya-ipc-terminal
        run: ./tuya-ipc-terminal --help

  docker-addon:
    runs-on: ubuntu-latest
    needs: [test-python, test-go]
    steps:
      - uses: actions/checkout@v4

      - name: Copy bridge source for Docker build
        run: cp -r tuya-ipc-terminal aventproxy-bridge-addon/tuya-ipc-terminal

      - name: Build add-on Docker image
        run: |
          docker build \
            --build-arg BUILD_FROM=debian:bookworm-slim \
            -t aventproxy-bridge:test \
            aventproxy-bridge-addon/

      - name: Verify binary in container
        run: |
          docker run --rm aventproxy-bridge:test tuya-ipc-terminal --help
